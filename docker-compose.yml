services:
  backend:
    build: ./backend
    ports:
      - "8006:8006"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
      